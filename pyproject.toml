[project]
name = "library"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "debugpy>=1.8.14",
]

[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["C9", "E", "F", "W", "I", "ANN"]
ignore = ["E741", "ANN001"]

